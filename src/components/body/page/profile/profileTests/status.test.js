import React from "react";
import { create, act } from "react-test-renderer";
import StatusBlock from './../StatusBlock';

describe("StatusBlock component", () => {
	test("status from props should be  ", () => {
		//let component;
		//act(() => {
		//	component = create(<StatusBlock userStatus="status was been received" />);
		//});
		//const instance = component.root;
		//const span = instance.findByType("span");
		//const div = instance.findByType("div");
		//const input = instance.findByType("input");
		//act(() => div.props.onDoubleClick());
		//act(() => input.value = "New Status");
		//expect(span.props.children).toBe("New Status");
	});
});


//instance => null         ?????????????